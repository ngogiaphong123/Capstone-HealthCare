export enum PatientProfileError {
    CONDITION_NOT_FOUND = 'Condition not found',
    HEALTH_RECORD_NOT_FOUND = 'Health record not found',
    HEALTH_RECORD_CONDITION_NOT_FOUND = 'Health record condition not found',
}
